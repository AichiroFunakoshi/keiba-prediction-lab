# Keiba Prediction Lab agent instructions

GitHubの`main`をコードと仕様の正とする。実レースデータ、学習済みモデル、予測成果物、結果、生成レポートは端末ローカル専用であり、明示的な再配布許諾がない限りコミット、push、アップロードしない。

## セッション開始時

1. `docs/HANDOFF.md`、`docs/PROJECT_PRINCIPLES.md`、`docs/LOCAL_RESULT_RECOVERY.md`を読む。
2. Git作業ツリーを確認する。未コミット変更がある場合は、所有者と内容を確認せず上書き、削除、stashしない。
3. 安全に同期できる場合だけ、GitHubの`main`へfast-forwardする。
4. 実データや生成物がGit管理から除外されていることを確認する。

## 過去予測・結果・精度改善を扱う場合

ユーザーが過去の予測、今回の結果、的中率、精度改善、振り返りを求めた場合は、モデル変更より先に次を自動実行する。

1. リポジトリ内の`local/`、`outputs/`、`reports/`、`artifacts/`、`models/`を確認する。
2. 見つからなければ、現在のリポジトリの親、`$HOME/keiba-prediction-lab`、`$HOME/競馬`、`$HOME/Documents`、`$HOME/Desktop`にある競馬プロジェクト候補を、ファイル名と予測バンドルの署名だけで読み取り専用探索する。
3. `.git`、`.Trash`、キャッシュ、メール、メッセージ、写真ライブラリ、認証情報領域など、競馬プロジェクトと無関係な場所を探索しない。上記候補で見つからない場合は、Mac全体を無差別に走査せず、探索済み範囲と追加で必要な保存場所をユーザーへ報告する。
4. 予測候補は、`manifest.json`、`actual.json`、`baseline-shadow.json`、`pace-shadow.json`、`bet-types-shadow.json`、`input-provenance.json`の6ファイルが同じディレクトリにある場合だけ予測バンドル候補とする。
5. 候補ごとに`audit-prediction-bundle`を実行し、成功したものだけを正式な発走前予測として扱う。ファイルを移動、名称変更、編集しない。
6. 同じディレクトリに監査可能な`bet-types-payouts.json`があるレースだけを、`diagnose-winner-misses`へ渡す。予測と結果のレースIDが一致しないものを混ぜない。
7. 元データを変更せず、診断レポートはGit除外済みの`reports/`へ新しい名前で保存する。既存レポートを上書きしない。
8. 実ファイルが見つからない場合、結果から過去予測を再構成しない。見つかった予測がチャット文面だけで正式バンドルでない場合も、正式パイプラインの事前予測と同等には扱わない。

診断結果は改善仮説の材料であり、個別開催の結果だけで係数、特徴量、閾値を自動更新しない。モデル変更は、今後の評価期間を含まない固定ウォークフォワード期間で現行モデルと比較してから行う。

## GitHubへ反映できるもの

復旧作業でGitHubへ反映してよいのは、汎用プログラム、合成テスト、データ契約、利用手順、出所・権利判断だけである。発見した実データの内容、ファイル、ローカル絶対パス、個人情報、認証情報はPRへ含めない。
